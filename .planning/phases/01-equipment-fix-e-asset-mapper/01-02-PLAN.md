---
phase: 01-equipment-fix-e-asset-mapper
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - app.js
  - style.css
  - tests/equipment-map.test.js
autonomous: false
requirements:
  - EQUIP-03
  - ARCH-01

must_haves:
  truths:
    - "Gli equipaggiamenti con immagine 404 mostrano un'icona SVG neutra (shield), non il quadratino con iniziale colorata"
    - "La funzione getAssetUrl() esiste e tutti i call site di _unitCdnUrl sono aggiornati"
    - "_unitCdnUrl non esiste piu' come nome funzione in app.js"
  artifacts:
    - path: "app.js"
      provides: "getAssetUrl() centralizzata + fallback SVG neutro"
      contains: "function getAssetUrl"
    - path: "style.css"
      provides: "Classe CSS per placeholder neutro SVG"
      contains: "profilo-unit-fallback--neutral"
    - path: "tests/equipment-map.test.js"
      provides: "Test EQUIP-03 e ARCH-01 aggiunti"
      min_lines: 60
  key_links:
    - from: "app.js (_renderEquipmentGrouped)"
      to: "app.js (getAssetUrl)"
      via: "chiamata diretta"
      pattern: "getAssetUrl\\("
    - from: "app.js (_renderUnits)"
      to: "app.js (getAssetUrl)"
      via: "chiamata diretta"
      pattern: "getAssetUrl\\("
---

<objective>
Sostituire il fallback colored-initial con SVG neutro (EQUIP-03) e rinominare _unitCdnUrl in getAssetUrl aggiornando tutti i call site (ARCH-01).

Purpose: Completare la pulizia visiva degli equipment senza immagine e centralizzare la funzione di lookup asset per le fasi future.
Output: Fallback SVG funzionante, getAssetUrl() unica funzione CDN, test completi.
</objective>

<execution_context>
@C:/Users/pedro/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pedro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-equipment-fix-e-asset-mapper/01-RESEARCH.md
@.planning/phases/01-equipment-fix-e-asset-mapper/01-01-SUMMARY.md

@app.js (righe 3756-3766: _unitCdnUrl, righe 3830-3846: unitCardHtml con fallback, righe 3869-3886: _renderUnits con fallback)
@style.css (cercare .profilo-unit-fallback per stile attuale)
@tests/equipment-map.test.js (test esistenti da Plan 01)
</context>

<interfaces>
<!-- Stato di app.js DOPO Plan 01 (costanti gia' corrette) -->

Da app.js riga ~3756 (da rinominare):
```javascript
function _unitCdnUrl(name, category) {
  if (UNIT_COC_SLUG[name]) {
    const {c, s} = UNIT_COC_SLUG[name];
    return `https://coc.guide/static/imgs/${c}/${s}.png`;
  }
  const CAT = {heroes:'hero',troops:'troop',spells:'spell',pets:'pet',equipment:'equipment'};
  const cat = CAT[category] || category || 'troop';
  const slug = name.toLowerCase().replace(/['.()]/g,'').replace(/\s+/g,'-').replace(/-+/g,'-');
  return `https://coc.guide/static/imgs/${cat}/${slug}.png`;
}
```

Call site 1 — riga ~3832 (in _renderEquipmentGrouped > unitCardHtml):
```javascript
const imgUrl = _unitCdnUrl(u.name, 'equipment');
```

Call site 2 — riga ~3871 (in _renderUnits):
```javascript
const imgUrl = _unitCdnUrl(u.name, cdnCategory);
```

Fallback attuale — riga ~3842-3843 (in unitCardHtml dentro _renderEquipmentGrouped):
```
onerror="this.style.display='none';this.nextElementSibling.style.display='flex'"
Div con class="profilo-unit-fallback" style="display:none;background:${fbColor}" contenuto ${fbInit}
```

Stesso pattern fallback in _renderUnits — riga ~3881-3882.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Aggiungere test EQUIP-03 e ARCH-01, implementare SVG placeholder e rinominare getAssetUrl</name>
  <files>tests/equipment-map.test.js, app.js, style.css</files>
  <behavior>
    - Test EQUIP-03: La funzione unitCardHtml (copiata) per un item fittizio produce HTML che contiene un tag svg nel fallback div e NON contiene _unitFallbackColor
    - Test ARCH-01a: getAssetUrl produce URL nel formato corretto per nomi mappati in UNIT_COC_SLUG
    - Test ARCH-01b: getAssetUrl auto-genera slug per nomi non mappati
  </behavior>
  <action>
Tre modifiche coordinate:

**1. Aggiungere test a tests/equipment-map.test.js:**

Aggiungere 3 test in fondo al file:

- Test `'EQUIP-03: fallback equipment usa SVG neutro, non colored-initial'`: Definire una funzione locale unitCardHtml che produce il template HTML TARGET (con SVG star nel fallback div, classe `profilo-unit-fallback--neutral`, senza fbColor/fbInit). Generare HTML per un item fittizio. Assert: html.includes('svg') e' true, html.includes('profilo-unit-fallback--neutral') e' true, html.includes('_unitFallbackColor') e' false.

- Test `'ARCH-01: getAssetUrl produce URL CDN corretti per item mappati'`: Definire una funzione locale getAssetUrl con un subset di UNIT_COC_SLUG (Barbarian Puppet e Battle Drill). Assert: getAssetUrl('Barbarian Puppet', 'equipment') === 'https://coc.guide/static/imgs/equipment/barbarian-puppet.png' e getAssetUrl('Battle Drill', 'troops') === 'https://coc.guide/static/imgs/troop/battle-drill.png'.

- Test `'ARCH-01b: getAssetUrl auto-genera slug per item non mappati'`: Definire getAssetUrl senza mappa. Assert: getAssetUrl('Some New Troop', 'troops') === 'https://coc.guide/static/imgs/troop/some-new-troop.png'.

**2. Modificare app.js — EQUIP-03: Sostituire fallback colored-initial con SVG neutro:**

In `_renderEquipmentGrouped` funzione `unitCardHtml` (riga ~3837-3843):
- Rimuovere le due righe che calcolano `fbColor` e `fbInit` (const fbColor = _unitFallbackColor(u.name) e const fbInit = ...)
- Sostituire il div fallback: togliere `style="display:none;background:${fbColor}"` e il contenuto `${fbInit}`, mettere al loro posto `class="profilo-unit-fallback profilo-unit-fallback--neutral" style="display:none"` con dentro un SVG stella a 5 punte (viewBox="0 0 24 24", fill="none", stroke="currentColor", stroke-width="1.5", path: "M12 3l-1.9 5.8H4.2l4.8 3.5-1.8 5.7L12 14.5l4.8 3.5-1.8-5.7 4.8-3.5h-5.9z")
- Il meccanismo onerror NON cambia

Fare lo STESSO cambio in `_renderUnits` (riga ~3876-3882): rimuovere `fbColor`/`fbInit` e sostituire il div fallback con la stessa versione SVG neutra.

NOTA: La funzione `_unitFallbackColor()` (riga ~3768) PUO' restare in app.js. Verificare con grep se ha altri call site. Se no, aggiungere commento `// TODO: rimuovere se non usata altrove`.

**3. Modificare app.js — ARCH-01: Rinominare _unitCdnUrl in getAssetUrl:**

a) Rinominare la dichiarazione: `function _unitCdnUrl(name, category)` diventa `function getAssetUrl(name, category)` (riga ~3756)
b) Aggiornare call site 1: `_unitCdnUrl(u.name, 'equipment')` diventa `getAssetUrl(u.name, 'equipment')` (riga ~3832)
c) Aggiornare call site 2: `_unitCdnUrl(u.name, cdnCategory)` diventa `getAssetUrl(u.name, cdnCategory)` (riga ~3871)
d) PRIMA di rinominare, eseguire `grep -n "_unitCdnUrl" app.js` per verificare tutti i call site (devono essere esattamente 3: definizione + 2 usi)
e) DOPO il rename, eseguire `grep -n "_unitCdnUrl" app.js` — deve restituire 0 risultati
f) NON cambiare la categoria `c:'troop'` di Battle Drill

**4. Modificare style.css — Aggiungere classe --neutral:**

Cercare `.profilo-unit-fallback` in style.css. Aggiungere DOPO il blocco esistente:

```css
.profilo-unit-fallback--neutral {
  background: transparent !important;
  color: #8e99a4;
  align-items: center;
  justify-content: center;
}
.profilo-unit-fallback--neutral svg {
  width: 60%;
  height: 60%;
}
```

Commit: `fix(01-02): EQUIP-03 svg placeholder, ARCH-01 rename getAssetUrl`
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
- npm test passa tutti i 20 test (11 pre-esistenti + 6 Plan 01 + 3 nuovi)
- grep "_unitCdnUrl" app.js restituisce 0 risultati
- grep "getAssetUrl" app.js restituisce la definizione + 2 call site
- grep "profilo-unit-fallback--neutral" style.css restituisce la classe CSS
- grep "profilo-unit-fallback--neutral" app.js restituisce le occorrenze nei template HTML
- Nessun fallback con colored-initial rimane in _renderEquipmentGrouped ne' in _renderUnits
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verifica visiva di tutti i fix Phase 1</name>
  <files>app.js</files>
  <action>Nessuna modifica automatica — questo task e' una verifica visiva umana di tutti i fix implementati in Phase 1 (Plan 01 e Plan 02). Aprire il sito e verificare i 5 success criteria della fase.</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>L'utente ha verificato visivamente tutti e 5 i fix e ha digitato "approved".</done>
  <what-built>Tutti e 5 i fix di Phase 1: Stick Horse sotto BK, Altro rimossa, Battle Drill immagine corretta, placeholder SVG neutro, getAssetUrl centralizzata</what-built>
  <how-to-verify>
    1. Aprire il sito in locale o su Vercel preview
    2. Navigare al profilo di un membro che ha Stick Horse equipaggiato
    3. Verificare: Stick Horse appare sotto "Re dei Barbari", NON in una sezione "Altro"
    4. Verificare: La sezione "Altro" non e' visibile da nessuna parte nel profilo equipaggiamenti
    5. Navigare al profilo di un membro con Battle Drill — verificare che l'immagine si carica (non broken)
    6. Verificare che eventuali equipment senza immagine CDN mostrano una stella SVG grigia (non un quadrato con lettera colorata)
  </how-to-verify>
  <resume-signal>Digita "approved" se tutto e' corretto, oppure descrivi i problemi riscontrati</resume-signal>
</task>

</tasks>

<verification>
```bash
# Tutti i test passano (20 totali)
npm test

# ARCH-01: nessun riferimento residuo a _unitCdnUrl
grep -c "_unitCdnUrl" app.js  # deve restituire 0

# ARCH-01: getAssetUrl presente
grep -n "getAssetUrl" app.js

# EQUIP-03: classe CSS presente
grep -n "profilo-unit-fallback--neutral" style.css

# EQUIP-03: SVG nel fallback (non colored-initial)
grep -n "profilo-unit-fallback--neutral" app.js
```
</verification>

<success_criteria>
1. 20 test passano con npm test (11 pre-esistenti + 9 nuovi totali)
2. Il fallback per immagini 404 mostra una stella SVG grigia, non il quadratino con iniziale colorata
3. La funzione getAssetUrl() e' l'unica funzione CDN per unit/equipment images — _unitCdnUrl non esiste piu'
4. Nessuna logica di fallback immagine duplicata tra _renderEquipmentGrouped e _renderUnits (entrambe usano lo stesso pattern SVG)
5. Verifica visiva umana conferma tutti e 5 i fix
</success_criteria>

<output>
After completion, create `.planning/phases/01-equipment-fix-e-asset-mapper/01-02-SUMMARY.md`
</output>
